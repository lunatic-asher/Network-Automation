---
- name: Config_pull
  hosts: aruba_switches
  gather_facts: false

  tasks:
    - name: Get Version
      ansible.netcommon.cli_command:
        command: show running-config
      register: output

    - name: Print Version
      debug:
        var: output.stdout_lines
